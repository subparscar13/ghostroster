# --- Node / Next.js (M2+) ---
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.vercel/

# --- Python / uv (M1 pipeline) ---
.venv/
__pycache__/
*.py[cod]
.uv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- Raw Lahman download cache (regenerated by the pipeline) ---
# NOTE: public/data/ is intentionally NOT ignored — generated JSON is
# committed per D-005 so the static deploy needs no pipeline run.
pipeline/.cache/

# --- Env / secrets ---
.env
.env.local
.env*.local

# --- OS / editor junk ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
