import { RunContainer } from "@/components/RunContainer";

/** The run: spin → draft → simulate → result. State lives in RunContainer (client). */
export default function Play() {
  return <RunContainer />;
}
