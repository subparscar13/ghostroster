import { RunContainer } from "@/components/RunContainer";

/** Daily challenge: date-seeded spins + sim seed (same for everyone), spoiler-safe
 * share. Replay allowed. */
export default function Daily() {
  return <RunContainer mode="daily" />;
}
