# Cloudflare Pages headers. Hashed Next assets are immutable; committed data JSON
# can change on redeploy, so cache it briefly with revalidation.
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable

/data/*
  Cache-Control: public, max-age=3600, must-revalidate
